FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY scheduler.py .

# Persistent state volume
VOLUME ["/data"]

EXPOSE 9000

CMD ["python", "-m", "uvicorn", "scheduler:app", "--host", "0.0.0.0", "--port", "9000"]
